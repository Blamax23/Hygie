@using Hygie.Model;

@model Contenu;

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Bienvenue</h1>
    <br />
    <br />
    <br />

    <div class="row justify-content-center">
        @foreach(var quiz in Model.Quizzes)
		{
			<div class="col-md-4 mb-4">
				<div class="card shadow-sm">
					<div class="card-body">
						<p>@quiz.Category</p>
					</div>
				</div>
			</div>
		}
    </div>
</div>

<style>
body {
    background-color: #f8f9fa; /* Couleur de fond douce */
    font-family: 'Arial', sans-serif; /* Police moderne */
    color: #333; /* Couleur de texte sombre */
    margin: 0; /* Suppression de la marge par défaut */
    padding: 20px; /* Espacement autour du contenu */
}

.text-center {
    margin-bottom: 40px; /* Espacement inférieur pour séparer des éléments suivants */
}

h1.display-4 {
    font-size: 3rem; /* Taille de police large */
    color: #0056b3; /* Couleur d'accent */
    margin-bottom: 20px; /* Espacement inférieur */
    text-transform: uppercase; /* Transforme le texte en majuscules */
    letter-spacing: 1px; /* Espacement entre les lettres */
}

/* Styles pour les cartes */
.card {
    border: none; /* Supprime la bordure par défaut */
    border-radius: 15px; /* Coins arrondis pour un aspect moderne */
    background-color: #ffffff; /* Couleur de fond blanche pour les cartes */
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15); /* Ombre douce et étendue pour la carte */
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Transition plus douce pour l'effet hover */
    overflow: hidden; /* Masque tout débordement pour les coins arrondis *
}

.card:hover {
    transform: translateY(-5px); /* Légère translation vers le haut au survol */
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2); /* Ombre plus prononcée au survol */
}

/* Styles pour le contenu des cartes */
.card-body {
    padding: 25px; /* Espacement interne pour le contenu */
    text-align: center; /* Centre le texte à l'intérieur de la carte */
}

.card-body h5 {
    font-size: 1.5rem; /* Taille de police pour le titre dans les cartes */
    color: #333; /* Couleur de texte sombre */
    margin-bottom: 10px; /* Espacement inférieur pour le titre */
    font-weight: bold; /* Met le titre en gras */
    text-transform: uppercase; /* Transforme le texte en majuscules */
}

.card-body p {
    font-size: 1rem; /* Taille de police pour le texte dans les cartes */
    color: #666; /* Couleur de texte pl


/* Styles pour la mise en page des colonnes */
.row {
    margin: 0; /* Suppression de la marge par défaut */
}

.col-md-4 {
    padding: 0 10px; /* Ajout d'un espacement entre les colonnes */
}

</style>
